[package]
name = "static-load"
version = "0.1.0"
edition = "2024"

[dependencies]
arc-swap = "1.9.0"
tokio = { version = "1.50.0", features = ["rt", "signal"] }

[features]
examples = ["tokio/macros"]
