[package]
name = "static-load"
version = "0.1.0"
edition = "2024"

[dependencies]
hazarc = "0.2.0"

# only for examples
tokio = { version = "1.50.0", features = [
    "rt",
    "signal",
    "macros",
], optional = true }

[features]
examples = ["dep:tokio"]
