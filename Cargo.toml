[package]
name = "static-load"
version = "0.1.0"
edition = "2024"

[dependencies]
hazarc = "0.2.0"

[dev-dependencies]
divan = "0.1.21"
# only for examples
tokio = { version = "1.50.0", features = ["rt", "signal", "macros"] }

[[bench]]
name = "load"
harness = false
